% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_dist.R
\name{map_dist_odkc}
\alias{map_dist_odkc}
\title{Map Disturbance observations interactively from ODK Central data.}
\usage{
map_dist_odkc(
  dist,
  tracks = NULL,
  sites = NULL,
  wastd_url = wastdr::get_wastd_url(),
  fmt = "\%d/\%m/\%Y \%H:\%M",
  tz = "Australia/Perth",
  cluster = FALSE
)
}
\arguments{
\item{dist}{The output of \code{parse_disturbance_observations()}.}

\item{tracks}{The output of `turtleviewer::turtledata$tracks_dist` -
Turtle nest disturbance obs joined to turtle nests.}

\item{sites}{An sf object of sites with `site_name` and polygon geom, e.g.
`turtleviewer::turtledata$sites`.}

\item{wastd_url}{The base URL for WAStD, default: \code{get_wastd_url()}}

\item{fmt}{The desired date format, default: "d/m/Y H:M"}

\item{tz}{The lubridate timezone, default: "Australia/Perth}

\item{cluster}{If TRUE, cluster map markers. Default: FALSE.
Note: In some places, the aerial background layer does not provide imagery
at sufficient zoom levels, and therefore restricts the map zoom at levels
where the cluster markers don't expand. Switch to "Place names" to let
cluster markers expand.}
}
\value{
A leaflet map
}
\description{
Map Disturbance observations interactively from ODK Central data.
}
\details{
Creates a Leaflet map with an interactive legend offering to toggle
each disturbance class separately. The maps auto-zooms to the extent of data
given.
}
